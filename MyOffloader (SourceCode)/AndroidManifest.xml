
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myoffloader"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.myoffloader.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="imageConversion"></activity>
        <activity android:name="nqueens"></activity>
        <activity android:name="LocalExecute"></activity>
        <activity android:name="OffExecute"></activity>
        <activity android:name="AdapExecute"></activity>
    </application>

</manifest>

